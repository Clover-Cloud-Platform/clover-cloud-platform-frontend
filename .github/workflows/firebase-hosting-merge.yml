# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - shell: bash
        env:
          SERVER_LINK: ${{ secrets.SERVER_LINK }}
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
        run: echo -e "REACT_APP_FIREBASE_API_KEY=$FIREBASE_API_KEY\nREACT_APP_FIREBASE_APP_ID=$FIREBASE_APP_ID\nREACT_APP_FIREBASE_AUTH_DOMAIN=$FIREBASE_AUTH_DOMAIN\nREACT_APP_FIREBASE_MEASUREMENT_ID=$FIREBASE_MEASUREMENT_ID\nREACT_APP_FIREBASE_MESSAGING_SENDER_ID=$FIREBASE_MESSAGING_SENDER_ID\nREACT_APP_FIREBASE_STORAGE_BUCKET=$FIREBASE_STORAGE_BUCKET\nREACT_APP_SERVER_LINK=$SERVER_LINK\nDANGEROUSLY_DISABLE_HOST_CHECK=true" >> ./.env
      - run: npm i && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CLOVER_CLOUD_PLATFORM }}'
          channelId: live
          projectId: clover-cloud-platform
